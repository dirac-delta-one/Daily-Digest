# Pinned to versions resolved on Python 3.12 (Phase 0.4).
# Regenerate with `pip freeze` after intentional upgrades.
# NOTE: keep PyPDF2 pinned for now — do not bump without measuring PDF
# extraction quality against the archive first (see Phase 3.3).
anthropic==0.109.2
google-api-python-client==2.197.0
google-auth-httplib2==0.4.0
google-auth-oauthlib==1.4.0
requests==2.34.2
yfinance==1.4.1
fredapi==0.5.2
sentence-transformers==5.6.0
faiss-cpu==1.14.3
PyPDF2==3.0.1
numpy==2.4.6
playwright==1.60.0
