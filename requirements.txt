# Pinned to versions resolved on Python 3.12 (Phase 0.4).
# Regenerate with `pip freeze` after intentional upgrades.
# NOTE: PyPDF2 3.0.1 -> pypdf 6.14.2 (Stage 2.6, 2026-07-10): extraction diffed
# on the real 11-PDF corpus (small, quality-positive changes — pypdf fixes
# spurious space-before-punct and split words), index rebuilt, eval re-baselined
# (see WORKLOG). Any future PDF-lib bump repeats that recipe.
anthropic==0.109.2
google-api-python-client==2.197.0
google-auth-httplib2==0.4.0
google-auth-oauthlib==1.4.0
requests==2.34.2
yfinance==1.4.1
fredapi==0.5.2
sentence-transformers==5.6.0
faiss-cpu==1.14.3
rank_bm25==0.2.2
pypdf==6.14.2
numpy==2.4.6
playwright==1.60.0
